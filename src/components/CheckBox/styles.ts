import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
  },

  checkBox: {
    width: 24,
    height: 24,
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 0.5,
  },

  text: {
    paddingLeft: 8,
    alignSelf: 'center',
  },
});
